## Example environment variables for Taskfile and GHCR publishing
# Copy this file to `.env` and adjust values as needed.

# GitHub Container Registry credentials
# Your GitHub username
GHCR_USERNAME=
# A Personal Access Token with at least `write:packages`
GHCR_TOKEN=

# Container image settings
# Default namespace/owner in GHCR (GitHub user or org). Defaults to metalagman in Taskfile
IMAGE_NAMESPACE=metalagman
# Image name used in Docker build/tag
IMAGE=tgbotspec
# Tag to push (default is latest)
TAG=latest

# Registry host (normally ghcr.io)
GHCR_REGISTRY=ghcr.io

# Output file for generated OpenAPI (used by Task "parse:save")
OUTPUT=openapi.generated.yaml

# Notes:
# - Task reads this file automatically (see Taskfile.yml `dotenv:` section).
# - Do NOT commit your real GHCR_TOKEN to version control. Keep it only in your local `.env`.